# ==============================================================================
# FRAPPE APP PRODUCTION DEPLOYMENT WORKFLOW (SENIOR EDITION)
# ==============================================================================
# Author: Senior DevOps/Developer
# Description: Bu workflow mavjud Frappe ilovasini xavfsiz yangilaydi.
#
# TUZATILGAN MUAMMO:
# - GitHub Repo nomi: "ruxsora_shirinlik"
# - Serverdagi Papka: "ext_accounts"
# - Yechim: APP_NAME o'zgaruvchisi "ext_accounts" ga qat'iy biriktirildi.
# ==============================================================================

name: Deploy to Production

# ------------------------------------------------------------------------------
# TRIGGER SOZLAMALARI
# ------------------------------------------------------------------------------
on:
  push:
    branches:
      - main              # main branchga kod tushganda ishlaydi
  workflow_dispatch:      # GitHub interfeysidan qo'lda ishlatish uchun

# ------------------------------------------------------------------------------
# GLOBAL O'ZGARUVCHILAR
# ------------------------------------------------------------------------------
env:
  # DIQQAT: Serverdagi papka nomini shu yerga yozing!
  APP_NAME: "ext_accounts"
  
  # Serverdagi yo'llar
  BENCH_PATH: "/home/frappe/frappe-bench"
  FRAPPE_USER: "frappe"

jobs:
  deploy:
    name: Production Server Update
    runs-on: ubuntu-latest
    timeout-minutes: 15  # Agar 15 daqiqada tugamasa, bekor qilinadi (xavfsizlik)

    steps:
      # ------------------------------------------------------------------------
      # 1. KODNI TEKSHIRISH VA TAYYORLASH
      # ------------------------------------------------------------------------
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Versiyalar tarixi to'liq olinadi

      # ------------------------------------------------------------------------
      # 2. SSH KALITLARINI SOZLASH
      # ------------------------------------------------------------------------
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      # ------------------------------------------------------------------------
      # 3. SIRLI MA'LUMOTLARNI (SECRETS) VALIDATSIYA QILISH
      # ------------------------------------------------------------------------
      - name: Validate Secrets
        run: |
          REQUIRED_SECRETS=("SERVER_IP" "SITE_NAME" "SSH_PRIVATE_KEY")
          for secret in "${REQUIRED_SECRETS[@]}"; do
            if [ -z "${!secret}" ] && [ -z "${{ secrets[secret] }}" ]; then
              # Izoh: GitHub Actionsda secretlarni bash loopda to'g'ridan-to'g'ri tekshirish qiyin,
              # shuning uchun oddiy manual tekshiruv tavsiya etiladi.
              echo "Checking configuration..."
            fi
          done
          
          if [ -z "${{ secrets.SERVER_IP }}" ]; then echo "❌ SERVER_IP missing"; exit 1; fi
          if [ -z "${{ secrets.SITE_NAME }}" ]; then echo "❌ SITE_NAME missing"; exit 1; fi
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then echo "❌ SSH_PRIVATE_KEY missing"; exit 1; fi
          
          echo "✅ Configuration validated successfully."

      # ------------------------------------------------------------------------
      # 4. DEPLOYMENT STATUSINI TEKSHIRISH
      # ------------------------------------------------------------------------
      - name: Check Server Status
        id: check_deploy
        run: |
          if ssh root@${{ secrets.SERVER_IP }} "[ -d ${{ env.BENCH_PATH }} ]"; then
            echo "deployment_exists=true" >> $GITHUB_OUTPUT
            echo "✅ Frappe Bench found on server."
          else
            echo "deployment_exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ Frappe Bench not found!"
          fi

      # ------------------------------------------------------------------------
      # 5. ASOSIY UPDATE JARAYONI (ENG MUHIM QISM)
      # ------------------------------------------------------------------------
      - name: Execute Server Update
        if: steps.check_deploy.outputs.deployment_exists == 'true'
        run: |
          echo "🚀 Starting deployment for app: ${{ env.APP_NAME }}"
          
          # SSH sessiyasini boshlaymiz.
          # Biz ENV o'zgaruvchilarni to'g'ridan-to'g'ri inject qilamiz.
          
          ssh root@${{ secrets.SERVER_IP }} << ENDSSH
            set -e  # Xatolik bo'lsa darhol to'xtatish
            
            echo "🔒 Logged in as root. Switching to frappe user..."
            
            # Frappe useriga o'tamiz va buyruqlarni bajaramiz
            su - ${FRAPPE_USER} << 'FRAPPE_EOF'
              set -e
              
              # O'zgaruvchilarni qabul qilish
              SITE_NAME="${{ secrets.SITE_NAME }}"
              BENCH_PATH="${{ env.BENCH_PATH }}"
              APP_DIR="${{ env.APP_NAME }}"  # <-- "ext_accounts" shu yerga keladi
              GIT_BRANCH="${{ github.ref_name }}"
              
              echo "📂 Navigating to bench: \${BENCH_PATH}"
              cd \${BENCH_PATH}
              
              # App papkasi borligini tekshirish
              if [ ! -d "apps/\${APP_DIR}" ]; then
                echo "❌ Error: App folder 'apps/\${APP_DIR}' not found!"
                echo "Please install the app manually first using: bench get-app ..."
                exit 1
              fi
              
              echo "🔄 Updating source code for: \${APP_DIR}"
              cd apps/\${APP_DIR}
              
              # Remote nomini aniqlash (origin yoki upstream)
              REMOTE_NAME=\$(git remote | head -1)
              echo "🔗 Git Remote: \${REMOTE_NAME}"
              
              # Kodni yangilash
              git fetch \${REMOTE_NAME}
              git reset --hard \${REMOTE_NAME}/\${GIT_BRANCH}
              
              echo "✅ Code updated to commit:"
              git log -1 --oneline
              
              # Orqaga - bench papkasiga qaytish
              cd \${BENCH_PATH}
              
              # 1. Python kutubxonalarni yangilash
              echo "📦 Installing Requirements..."
              bench setup requirements --python || true
              
              # 2. Frontend Assets (JS/CSS) ni qurish
              echo "🎨 Building Assets..."
              bench build --app \${APP_DIR}
              
              # 3. Migratsiya (DB o'zgarishlari)
              echo "🗄️ Migrating Database..."
              bench --site \${SITE_NAME} migrate
              
              # 4. Keshni tozalash
              echo "🧹 Clearing Cache..."
              bench --site \${SITE_NAME} clear-cache
              bench --site \${SITE_NAME} clear-website-cache
              
              echo "✅ Frappe User Tasks Completed!"
            FRAPPE_EOF
            
            # Root huquqlari bilan servislarni restart qilish
            echo "🔄 Restarting System Services..."
            supervisorctl restart all
            systemctl reload nginx || true
            
            echo "✅ DEPLOYMENT FINISHED SUCCESSFULLY!"
          ENDSSH

      # ------------------------------------------------------------------------
      # 6. YAKUNIY TEKSHIRUV (HEALTH CHECK)
      # ------------------------------------------------------------------------
      - name: Verify Deployment
        if: success()
        run: |
          echo "🏥 Performing Health Check..."
          sleep 15 # Servislar yonishi uchun biroz kutamiz
          
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.SERVER_IP }} || echo "000")
          echo "🌐 Site Status Code: $HTTP_CODE"
          
          if [[ "$HTTP_CODE" =~ ^(200|301|302)$ ]]; then
             echo "✅ Site is UP and Running!"
          else
             echo "⚠️ Warning: Site check returned $HTTP_CODE (Check Nginx logs)"
          fi

      # ------------------------------------------------------------------------
      # 7. NOTIFIKATSIYA (LOG)
      # ------------------------------------------------------------------------
      - name: Deployment Summary
        if: always()
        run: |
          echo "========================================="
          echo "App: ${{ env.APP_NAME }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Status: ${{ job.status }}"
          echo "========================================="